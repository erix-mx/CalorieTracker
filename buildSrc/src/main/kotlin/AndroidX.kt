object AndroidX {
    object Versions {
        const val KTX_VERSION = "1.10.0"
        const val LIFECYCLE_VERSION = "2.6.1"
    }

    object Core {
        const val KTX = "androidx.core:core-ktx:${Versions.KTX_VERSION}"

    }

    object Lifecycle {
        const val RUNTIME = "androidx.lifecycle:lifecycle-runtime-ktx:${Versions.LIFECYCLE_VERSION}"
    }
}